#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;
typedef long long int lli;
int main()
{
    lli testcases;
    cin >> testcases;
    for (lli i = 0; i < testcases; i++)
    {
        lli noe, val;
        vector<lli> v;
        cin >> noe;
        for (lli j = 0; j < noe; j++)
        {
            cin >> val;
            v.push_back(val);
        }
        //code
    }
    return 0;
}